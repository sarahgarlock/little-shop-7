<h3><%= @merchant.name %></h3>

<div id="merchants-items">
  <p><% @items.each do |item| %></p>
  <p><%= link_to item.name, "/merchants/#{@merchant.id}/items/#{item.id}" %>
  <%= button_to "Disable/Enable Item", "/merchants/#{@merchant.id}/items", method: :patch  %></p>
  <% end %>
</div>